/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['uwaterloo.ca'], // Add this line to allow images from the UW domain
    },
  }
  
  module.exports = nextConfig
  